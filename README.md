My Vim Setup ! :smiley: 
```bash
git clone http://github.com/tworitdash/.vim ~/.vim
```
Then,
```bash
mv ~/.vim/.vimrc ~/.vimrc
```
All set ! 

For padawans: read the `.vimrc` for reference and use it. 
